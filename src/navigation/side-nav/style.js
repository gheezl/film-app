const style = {
    sideBarBorder: {
        border: "none"
    },
    setColor: (color) => ({
        color: color
    }),
    setBackgroundColor: (backgroundColor) => ({
        backgroundColor: backgroundColor
    })
}

export default style;
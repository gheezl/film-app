const style = {
    navBorder: {
        position: 'absolute',
        bottom: 0,
        left: 0,
        zIndex: 1,
        width: '100%',
    },
    setBackgroundColor: (backgroundColor) => ({
        backgroundColor: backgroundColor
    }),
    setColor: (color) => ({
        color: color
    })
}

export default style;
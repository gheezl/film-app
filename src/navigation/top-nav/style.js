const style = {
    border: (color) => ({
        borderBottom: `1px solid ${color}`,
        margin: "10px"
    }),
    textBorder: {
        display: "flex",
        justifyContent: "center",
        margin: "15px"
    }
}

export default style;